<!--pages/profile/profile.wxml - 个人中心-->
<view class="profile-container">
  <!-- 用户卡片 -->
  <view class="user-card">
    <view class="user-avatar avatar-placeholder" wx:if="{{!userInfo.avatarUrl}}">
      <text>{{userInfo.nickname[0] || '游'}}</text>
    </view>
    <image class="user-avatar" src="{{userInfo.avatarUrl}}" wx:if="{{userInfo.avatarUrl}}" />
    <view class="user-info">
      <text class="user-name">{{userInfo.nickname || '游客'}}</text>
      <text class="user-id">ID: {{userInfo.id || '未登录'}}</text>
    </view>
  </view>

  <!-- 数据面板 -->
  <view class="stats-panel">
    <view class="stat-item">
      <text class="stat-value">{{userInfo.wins || 0}}</text>
      <text class="stat-label">胜场</text>
    </view>
    <view class="stat-item">
      <text class="stat-value">{{userInfo.coins || 0}}</text>
      <text class="stat-label">金币</text>
    </view>
    <view class="stat-item">
      <text class="stat-value">{{winRate || '0%'}}</text>
      <text class="stat-label">胜率</text>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-list">
    <view class="menu-item" bindtap="onGameRecord">
      <text class="menu-icon">📊</text>
      <text class="menu-text">战绩记录</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="onAchievements">
      <text class="menu-icon">🏆</text>
      <text class="menu-text">成就系统</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="onSettings">
      <text class="menu-icon">⚙️</text>
      <text class="menu-text">设置</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="onAbout">
      <text class="menu-icon">ℹ️</text>
      <text class="menu-text">关于</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="onLogout">
      <text class="menu-icon">🚪</text>
      <text class="menu-text">退出登录</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>
</view>